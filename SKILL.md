---
name: jp-lyrics-study
description: 生成"日语歌词逐句学习网页"。当用户给出日语歌曲名（可含歌手名/乐队名），要求"做歌词学习网页""逐句讲解歌词""生成日语学习HTML""日语歌词注音/分词/语法讲解页"时使用。流程：先检索官方歌词发给用户确认，确认后按词法/语法/翻译逐句拆解，生成含逐句讲解、可点击生词弹窗（词性/形态/读音/释义/出处）、按 JLPT 分级生词总表的自包含 HTML 页面。面向初学日语但要求精确语法讲解的学习者。
---

# 日语歌词学习网页

为指定日语歌曲生成一个自包含的单文件 HTML：逐句显示歌词（带 `<ruby>` 振假名）、点击可展开该句的分词/语法/翻译，所有生词可点击弹出词性/形态/读音/释义/出处，底部生词总表按 JLPT 等级分组去重。

## 工作流（Workflow）

### 第 1 步：获取并确认歌词

1. 用搜索检索用户指定歌曲的**官方日文歌词**（关键词：`<歌名> 歌詞 <歌手>`，必要时附 `lyrics`/`歌詞 全文`）。
2. 把检索到的歌词原文**完整发给用户**，明确请其确认「是否是这首歌」。**未获确认前不得进入生成**；用户说不是则换渠道重新检索。

### 第 2 步：读取填充指南与模板

- 读取 `references/data-guide.md`，掌握 `S` 数组与 `POS` 表的确切结构与读音/去重规则。
- 读取 `assets/lyric-page-template.html`（含全部样式、交互、渲染逻辑），确认模板中两处占位：`TODO: 填充歌词数据`（`const S=[...]`）与 `TODO: 填充词性与活用`（`const POS={...}`）。

### 第 3 步：逐句拆分并填数据

按 data-guide.md 逐句处理：
- 每句拆成 `jp`（带 `<ruby>`）、`seg`（分词）、`tr`（翻译）、`gram`（语法）、`ws`（本句生词数组）。
- 填 `POS` 表：所有词给 `pos`；**仅** 动词/形容词/助动词/句型 给 `form`+`forms`（其他词只需 `pos`）。
- 遵守读音规则：纯平假名词读音留空；片假名词读音转平假名；说明行只写与词性/形态/原形不重复的内容，无则留空。
- 重复出现的词在 `POS` 只写一次（模板会自动标注出处）。

### 第 4 步：生成并交付

1. 复制模板到输出目录，改名 `歌词名-歌词学习.html`，替换 `S` 与 `POS` 占位。
2. 替换 hero 元信息（模板中 `[歌名]`/`[歌手名]`/`[歌名中文译名]`/`[作词人]`/`[一句歌词主题/简介]` 共 5 处占位符）。
3. 用 `html` skill 的 `scripts/shot.py <html>` 自检（确认无 JS 报错、桌面+移动端布局正常、hero 显示本首歌信息）。
4. 用 `present_files` 交付。

## 关键规则（不可破坏）

- **词性**：所有词都要标。
- **形态**：仅 动词/形容词/助动词/句型 标（含动词短语、使役、敬语动词）。
- **浮层读音**：纯平假名词（无汉字）不显示读音行；片假名词读音显示为平假名。
- **浮层出处**：多出处词直接写明每处句子内容（不要跳转链接）。
- **其他形态**：tag 用 flex-wrap + gap:4px 控制间距，tag 内不换行。
- **说明行**：与词性/形态/原形重复的内容不显示；无说明时隐藏整行。
- **生词总表**：词性与形态放在生词按钮内部；按 JLPT 分组去重。
- **hero 氛围**：每首歌按歌词意象同时定制 hero 背景配色与动画特效（如 夜蓝泡沫 / 深夜→破晓微光+飘雪 / 花瓣 / 星光…），配色与特效都要贴合本首歌情绪，不要沿用上一首。
- **展开箭头**：用内联 SVG，不用字符。

## 资源

- `assets/lyric-page-template.html`：成品页面模板（样式/交互/渲染完整，仅数据留占位）。生成时复制并替换数据。
- `references/data-guide.md`：`S`/`POS` 的数据结构、读音与去重规则、完整示例。填数据前必读。
